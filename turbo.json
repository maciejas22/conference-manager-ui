{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": [
    "SUPABASE_URL",
    "SUPABASE_ANON_KEY",
    "ANALYZE",
    "NEXT_PUBLIC_API_URL",
    "NEXT_PUBLIC_UI_GQL_PROXY_URL",
    "NEXT_PUBLIC_MAIN_ZONE_DOMAIN",
    "NEXT_PUBLIC_MAIN_ZONE_BASE_PATH",
    "NEXT_PUBLIC_USER_MANAGEMENT_ZONE_DOMAIN",
    "NEXT_PUBLIC_USER_MANAGEMENT_ZONE_BASE_PATH",
    "NEXT_PUBLIC_CONFERENCE_MANAGEMENT_ZONE_DOMAIN",
    "NEXT_PUBLIC_CONFERENCE_MANAGEMENT_ZONE_BASE_PATH"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^gen-types", "^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env", ".env.*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "gen-schema": {
      "cache": false
    },
    "gen-types": {
      "cache": false
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "prettier-check": {},
    "start": {
      "dependsOn": ["^gen-types", "^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env", ".env.*"]
    },
    "type-check": {
      "dependsOn": ["^gen-types"]
    },
    "test": {},
    "test:watch": {
      "cache": false,
      "persistent": true
    }
  }
}
